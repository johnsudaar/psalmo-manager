<h1 class="text-2xl font-semibold text-gray-900">Tableau de bord</h1>
<p class="mt-2 text-sm text-gray-500">
  <% if current_edition %>
    <%= current_edition.name %>
  <% else %>
    Aucune édition disponible. <% # link to create one once editions controller exists %>
  <% end %>
</p>
