<div class="max-w-xl">
  <div class="flex items-center gap-4 mb-6">
    <%= link_to "← Retour à la fiche", staff_profile_path(@staff_profile),
          class: "text-sm text-gray-500 hover:text-gray-700" %>
    <h1 class="text-2xl font-semibold text-gray-900">Modifier — <%= @staff_profile.full_name %></h1>
  </div>
  <div class="bg-blue-50 border border-blue-200 rounded p-4 text-sm text-blue-800">
    Les champs se sauvegardent automatiquement depuis la fiche.
    <%= link_to "Voir la fiche", staff_profile_path(@staff_profile),
          class: "font-medium underline hover:text-blue-900" %>
  </div>
</div>
